android.useAndroidX=true
kotlin.code.style=official
# CI runners saw daemon metaspace exhaustion with the 512 MiB default, which
# killed :app:packageRelease mid-packaging. Give the daemon explicit headroom.
org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
